/* Create a list named my_array, which should contain numeric values [3, 132, 14].

Use console.log() to print the first item in my_array (ie item 0).

Create a new list with an optional name. Leave it blank.

Use push() to add the strings “hello” and “world!” in the last array.

Use console.log() to print the entire last array. */


var my_array= [3,132,14];
console.log(my_array[0]);
console.log(my_array[1]);
console.log(my_array[2]);

var array2=[];
array2.push("hello");
array2.push("world");
console.log(array2);
